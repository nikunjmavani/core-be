# GENERATED by pnpm tool:generate-project-identity — do not edit; change tooling/setup/setup.config.json instead.
name: Setup Project Identity
description: Sets project identity environment variables for the current job.
runs:
  using: composite
  steps:
    - name: Set project identity env vars
      shell: bash
      run: |
        echo "PROJECT_SLUG=core-be" >> "$GITHUB_ENV"
        echo "PROJECT_DISPLAY_NAME=core-be" >> "$GITHUB_ENV"
        echo "API_IMAGE=core-be-api" >> "$GITHUB_ENV"
        echo "WORKER_IMAGE=core-be-worker" >> "$GITHUB_ENV"
        echo "DOCKER_LOCAL_API_TAG=core-be" >> "$GITHUB_ENV"
        echo "GHCR_CACHE_SCOPE_API=core-be-api" >> "$GITHUB_ENV"
        echo "GHCR_CACHE_SCOPE_WORKER=core-be-worker" >> "$GITHUB_ENV"
        echo "GIT_DEFAULT_BRANCH=main" >> "$GITHUB_ENV"
        echo "GIT_PRODUCTION_BRANCH=main" >> "$GITHUB_ENV"
        echo "GIT_NON_PRODUCTION_BRANCH=main" >> "$GITHUB_ENV"
        echo 'PROTECTED_BRANCHES_JSON=["main"]' >> "$GITHUB_ENV"
        echo 'BRANCH_ENV_MAP_JSON={"main":"production"}' >> "$GITHUB_ENV"
