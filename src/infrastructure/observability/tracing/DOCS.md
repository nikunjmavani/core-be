`src/infrastructure/observability/tracing/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Tracing — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Utilities

### `captureTraceContextForPropagation`

Source: `src/infrastructure/observability/tracing/trace-context.util.ts`

Captures the active span's trace context for injection into BullMQ job data.
Returns an empty object when no span is active.

### `runWithPropagatedTraceContext`

Source: `src/infrastructure/observability/tracing/trace-context.util.ts`

Runs work as a consumer span linked to a propagated parent (e.g. mail worker).
When `traceparent` is absent, runs the callback without creating a span.

### `TraceContextCarrier`

Source: `src/infrastructure/observability/tracing/trace-context.util.ts`

W3C trace context fields stored on BullMQ job payloads for cross-process propagation.

## Other

### `initOpenTelemetry`

Source: `src/infrastructure/observability/tracing/otel.ts`

Optional OpenTelemetry export to OTLP. Coexists with Sentry (errors + optional Sentry traces).
When OTEL_EXPORTER_OTLP_ENDPOINT is unset, this is a no-op.

### `resetOpenTelemetryStateForTests`

Source: `src/infrastructure/observability/tracing/otel.ts`

Test-only reset so init/shutdown can be exercised in isolation.

### `shutdownOpenTelemetry`

Source: `src/infrastructure/observability/tracing/otel.ts`

<!-- MISSING_DESCRIPTION -->

