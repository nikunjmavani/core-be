`src/infrastructure/queue/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Queue — Catalog

## Overview

BullMQ + Redis runtime for the platform. Owns connection management, the worker bootstrap, the centralized scheduler for repeatable retention jobs, the per-queue dead-letter sibling, and the worker-runtime helpers (RLS context, heartbeat, health server, shutdown timing). Domain-owned queues and processors live in their respective domain folders — this module never holds business logic, only the runtime substrate.

→ Read the full overview: [OVERVIEW.md](./OVERVIEW.md)

## Other

### `registerDomainWorkers`

Source: `src/infrastructure/queue/bootstrap.ts`

<!-- MISSING_DESCRIPTION -->

### `stopRssMonitoring`

Source: `src/infrastructure/queue/bootstrap.ts`

Stop RSS monitoring.

### `WorkerHandle`

Source: `src/infrastructure/queue/bootstrap.ts`

<!-- MISSING_DESCRIPTION -->

### `getBullMQConnectionOptions`

Source: `src/infrastructure/queue/connection.ts`

BullMQ connection options for use when creating Queue/Worker.
Uses REDIS_URL by default, with REDIS_BULLMQ_URL available as an explicit override.

TLS is intentionally not configured: production traffic flows over Railway's
private network (`redis://*.railway.internal`), which is already isolated and
does not terminate `rediss://`. `family: 0` enables dual-stack DNS lookup so
IPv6-only private hostnames resolve correctly.

### `pingBullMQ`

Source: `src/infrastructure/queue/health.ts`

Verifies BullMQ can reach Redis using the notification queue connection.
All queues reuse the same BullMQ Redis options; one representative probe is sufficient.

### `parseQueueDashboardMutation`

Source: `src/infrastructure/queue/queue-dashboard.ts`

Maps a Bull Board API path (under /admin/queues) and HTTP method to an audit action.
Returns null when the request should not be audited (e.g. read-only).

### `QueueDashboardMutationAudit`

Source: `src/infrastructure/queue/queue-dashboard.ts`

<!-- MISSING_DESCRIPTION -->

### `registerQueueDashboard`

Source: `src/infrastructure/queue/queue-dashboard.ts`

Registers the Bull Board queue dashboard at /admin/queues.
Protected by JWT + requireRole(SUPER_ADMIN).
Mutating Bull Board API calls (2xx) are written to audit.logs.
Only registered when ENABLE_QUEUE_DASHBOARD is true.

### `RegisterQueueDashboardDeps`

Source: `src/infrastructure/queue/queue-dashboard.ts`

<!-- MISSING_DESCRIPTION -->

### `getScheduledJobs`

Source: `src/infrastructure/queue/scheduler.ts`

Canonical list of repeatable cleanup jobs (queue identity from domain constants).

### `registerScheduledJobs`

Source: `src/infrastructure/queue/scheduler.ts`

Registers repeatable jobs with BullMQ. No-op when SCHEDULER_ENABLED is false.

### `RegisterScheduledJobsOptions`

Source: `src/infrastructure/queue/scheduler.ts`

<!-- MISSING_DESCRIPTION -->

### `ScheduledJob`

Source: `src/infrastructure/queue/scheduler.ts`

<!-- MISSING_DESCRIPTION -->

### `SchedulerHandle`

Source: `src/infrastructure/queue/scheduler.ts`

<!-- MISSING_DESCRIPTION -->

