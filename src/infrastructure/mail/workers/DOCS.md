`src/infrastructure/mail/workers/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Workers — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Workers

### `createMailOutboxSweeperWorker`

Source: `src/infrastructure/mail/workers/mail-outbox-sweeper.worker.ts`

Re-enqueues mail_outbox rows stuck in `pending` longer than the configured threshold.
Repeatable schedule: `src/infrastructure/queue/scheduler.ts`.

<!-- MISSING_REMARKS -->

Public export in `mail-outbox-sweeper.worker.ts` requires a `@remarks` block.

### `createMailWorker`

Source: `src/infrastructure/mail/workers/mail.worker.ts`

Creates a BullMQ worker that processes email send jobs.

<!-- MISSING_REMARKS -->

Public export in `mail.worker.ts` requires a `@remarks` block.

## Processors

### `MailOutboxSweeperJobResult`

Source: `src/infrastructure/mail/workers/mail-outbox-sweeper.processor.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `mail-outbox-sweeper.processor.ts` requires a `@remarks` block.

### `runMailOutboxSweeperJob`

Source: `src/infrastructure/mail/workers/mail-outbox-sweeper.processor.ts`

Re-enqueues mail_outbox rows stuck in `pending` longer than the configured threshold.

<!-- MISSING_REMARKS -->

Public export in `mail-outbox-sweeper.processor.ts` requires a `@remarks` block.

### `processMailOutboxJob`

Source: `src/infrastructure/mail/workers/mail.processor.ts`

Processes a single mail outbox job (idempotent when status is already `sent`).

<!-- MISSING_REMARKS -->

Public export in `mail.processor.ts` requires a `@remarks` block.

### `ProcessMailOutboxJobOptions`

Source: `src/infrastructure/mail/workers/mail.processor.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `mail.processor.ts` requires a `@remarks` block.

### `ProcessMailOutboxJobResult`

Source: `src/infrastructure/mail/workers/mail.processor.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `mail.processor.ts` requires a `@remarks` block.

## Constants

### `MAIL_OUTBOX_SWEEPER_QUEUE_NAME`

Source: `src/infrastructure/mail/workers/mail-outbox-sweeper.constants.ts`

BullMQ queue name — repeatable schedule: `src/infrastructure/queue/scheduler.ts`.

