`src/infrastructure/database/contexts/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Contexts — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Contexts

### `withOrganizationDatabaseContext`

Source: `src/infrastructure/database/contexts/organization-database.context.ts`

Scoped RLS helper for the post-`DATABASE_RLS_SCOPED_CONTEXTS` migration (production hardening item 2).

Opens a short Drizzle transaction, sets `app.current_organization_id` via `SET LOCAL`, and
runs `callback` with a pinned `databaseHandle`. When the caller is already inside a worker
`withOrganizationContext(...)` (or a legacy request-pinned `organizationRequestDatabaseStorage`
session for the same organization) the existing handle is reused — no nested top-level
transaction, no extra pool checkout, no lost `SET LOCAL`.

Use from services and controllers for the **service-layer unit of work**, not the entire HTTP
request. External I/O (Stripe API, S3, Resend) **must not** run inside this callback — phase
such work into separate short transactions to avoid holding a pool checkout across network
round trips. This is the contract enforced by the `no-restricted-syntax` ESLint guardrail.

### `getOrganizationRequestDatabaseSession`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `getRequestDatabase`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `OrganizationRequestDatabaseSession`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

Fastify HTTP requests that send `X-Organization-Id` run inside a single Drizzle
transaction with `SET LOCAL app.current_organization_id` so every query shares one
checkout from the postgres.js pool and RLS policies see a stable GUC.

Workers must use context wrappers that pin ALS via `runWithPinnedOrganizationDatabaseSession`
and pass the explicit `databaseHandle` into processors/repositories — never rely on the
global pool fallback from `getRequestDatabase()` outside a pinned session.

### `organizationRequestDatabaseStorage`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `RequestScopedPostgresDatabase`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `runWithPinnedDatabaseHandle`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

Pins `databaseHandle` in ALS for retention/session workers without a tenant public id.

### `runWithPinnedOrganizationDatabaseSession`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

Pins `databaseHandle` in ALS so `getRequestDatabase()` returns the same checkout for
tenant-scoped worker and HTTP organization RLS transactions.

### `setLocalDatabaseConfig`

Source: `src/infrastructure/database/contexts/request-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `GlobalRetentionCleanupDatabaseContextOptions`

Source: `src/infrastructure/database/contexts/retention-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `withGlobalRetentionCleanupDatabaseContext`

Source: `src/infrastructure/database/contexts/retention-database.context.ts`

Runs a callback inside a transaction with `SET LOCAL app.global_retention_cleanup = true`
so tombstone and cross-tenant retention workers can access FORCE RLS tables under `core_be_app`.

### `withOrganizationContext`

Source: `src/infrastructure/database/contexts/tenant-database.context.ts`

Runs a callback inside a transaction with RLS organization context set via SET LOCAL.
Use for workers/scripts that must enforce tenant isolation at the database layer.

When the caller is already inside an HTTP request scoped by
`organization-rls-transaction.middleware` for the same `organizationPublicId`, this
reuses the active Drizzle handle instead of opening a nested top-level transaction
(which would use another pool checkout and lose the outer `SET LOCAL`).

The callback receives the pinned `databaseHandle` — pass it into worker repositories
via `createWorker*Repository(databaseHandle)` factories.

### `withSessionPublicIdDatabaseContext`

Source: `src/infrastructure/database/contexts/user-database.context.ts`

Sets `app.current_session_public_id` for cookie-based refresh (no JWT yet).

### `withSessionRetentionCleanupDatabaseContext`

Source: `src/infrastructure/database/contexts/user-database.context.ts`

Allows cross-user session retention deletes from the cleanup worker.

### `withSessionTokenHashDatabaseContext`

Source: `src/infrastructure/database/contexts/user-database.context.ts`

Sets `app.current_session_token_hash` for bearer-token logout revocation.

### `withUserDatabaseContext`

Source: `src/infrastructure/database/contexts/user-database.context.ts`

Sets `app.current_user_id` (auth.users public_id) for user-scoped RLS policies.
Reuses the active organization RLS transaction when present.

### `assertWorkerDatabaseContext`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

Asserts a worker job has pinned database context before touching Postgres.
No-op outside worker runtime.

### `assertWorkerForceRlsTableAccess`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

Asserts worker access to a FORCE RLS table uses an appropriate context kind.
No-op outside worker runtime.

### `getWorkerDatabaseContext`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `isWorkerRuntime`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `runWithWorkerDatabaseContext`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `withSystemTableWorkerContext`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

Explicit bypass for tables without tenant RLS (mail outbox, Stripe webhook ledger).
Pins ALS so getRequestDatabase() resolves in worker runtime without opening a transaction.

### `WorkerDatabaseContext`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `workerDatabaseContextForOrganization`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `workerDatabaseContextForUser`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `WorkerDatabaseContextKind`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

### `workerDatabaseContextStorage`

Source: `src/infrastructure/database/contexts/worker-database.context.ts`

<!-- MISSING_DESCRIPTION -->

## Errors

### `WorkerDatabaseContextError`

Source: `src/infrastructure/database/contexts/worker-database.context.error.ts`

Thrown when a worker process accesses Postgres without a pinned worker database context
(organization, retention, user, session cleanup, or system-table bypass).

## Utilities

### `assertWorkerRlsGucSet`

Source: `src/infrastructure/database/contexts/worker-database-guard.util.ts`

Verifies the Postgres session GUC for the active worker context kind is set (non-empty).
Call from createWorker*Repository factories before tenant-scoped queries.

### `resolveRepositoryDatabaseHandle`

Source: `src/infrastructure/database/contexts/worker-database-guard.util.ts`

Resolves the Drizzle handle for repositories. In worker runtime, requires an explicit
handle or a pinned ALS session from a context wrapper.

