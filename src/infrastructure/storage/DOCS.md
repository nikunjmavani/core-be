`src/infrastructure/storage/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Storage — Catalog

## Overview

S3-compatible object storage client. Owns the AWS SDK v3 client instance, presigned URL issuance for uploads + downloads, and HEAD-object validation for the upload-confirm flow. Used by [upload](src/domains/upload/) and [user-data-export](src/domains/user/sub-domains/user-data-export/).

→ Read the full overview: [OVERVIEW.md](./OVERVIEW.md)

## Services

### `createPresignedUploadUrl`

Source: `src/infrastructure/storage/storage.service.ts`

Generate a presigned PUT URL for direct client uploads to S3.

<!-- MISSING_REMARKS -->

Public export in `storage.service.ts` requires a `@remarks` block.

### `deleteObject`

Source: `src/infrastructure/storage/storage.service.ts`

Delete an object from S3. Returns false when the delete fails (caller may still tombstone the row).

<!-- MISSING_REMARKS -->

Public export in `storage.service.ts` requires a `@remarks` block.

### `getObjectUrl`

Source: `src/infrastructure/storage/storage.service.ts`

Build the public URL for an S3 object.

<!-- MISSING_REMARKS -->

Public export in `storage.service.ts` requires a `@remarks` block.

### `headObject`

Source: `src/infrastructure/storage/storage.service.ts`

Verify an uploaded object exists and check its content type via HeadObject.

<!-- MISSING_REMARKS -->

Public export in `storage.service.ts` requires a `@remarks` block.

### `putObjectBuffer`

Source: `src/infrastructure/storage/storage.service.ts`

Upload a buffer to S3 (server-side writes, e.g. audit export).

<!-- MISSING_REMARKS -->

Public export in `storage.service.ts` requires a `@remarks` block.

## Other

### `ObjectStoragePort`

Source: `src/infrastructure/storage/object-storage.port.ts`

Port for object storage (S3-compatible). Domain upload code depends on this abstraction.

### `PresignedUploadPost`

Source: `src/infrastructure/storage/object-storage.port.ts`

Browser-postable presigned upload: a form `url` plus hidden `fields` to submit with the file.

### `UploadedObjectMetadata`

Source: `src/infrastructure/storage/object-storage.port.ts`

<!-- MISSING_DESCRIPTION -->

### `getDefaultS3ObjectStorageAdapter`

Source: `src/infrastructure/storage/s3-adapter.ts`

<!-- MISSING_DESCRIPTION -->

### `S3ObjectStorageAdapter`

Source: `src/infrastructure/storage/s3-adapter.ts`

Default S3 implementation of {@link ObjectStoragePort}.

