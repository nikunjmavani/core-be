`src/shared/utils/auth/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Auth — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Utilities

### `requireOrganizationPermission`

Source: `src/shared/utils/auth/authorization.util.ts`

Returns a Fastify preHandler that checks the authenticated user has a specific
permission within the organization identified by the route param.

Looks up: user -> membership -> role -> role_permissions

> TSDoc parser warnings:
> - (5,20): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag
> - (5,34): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag
> - (5,42): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag
> - (11,6): Expecting a TSDoc tag starting with "{@"
> - (11,109): The "}" character should be escaped using a backslash to avoid confusion with a TSDoc inline tag

### `requireRole`

Source: `src/shared/utils/auth/authorization.util.ts`

Returns a Fastify preHandler that checks the authenticated user's global role.
Throws ForbiddenError if the user's role is not in the allowed list.

Usage:
  { preHandler: [app.authenticate, requireRole(GLOBAL_ROLES.SUPER_ADMIN, GLOBAL_ROLES.ADMIN)] }

> TSDoc parser warnings:
> - (6,6): Expecting a TSDoc tag starting with "{@"
> - (6,98): The "}" character should be escaped using a backslash to avoid confusion with a TSDoc inline tag

### `resolveAccessTokenRoleForUser`

Source: `src/shared/utils/auth/global-admin-role.util.ts`

JWT role for session issuance: global admin override, else user when ACTIVE.

### `resolveGlobalRoleForEmail`

Source: `src/shared/utils/auth/global-admin-role.util.ts`

Returns super_admin when email is listed in GLOBAL_ADMIN_EMAILS; otherwise undefined.

