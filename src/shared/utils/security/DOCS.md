`src/shared/utils/security/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Security — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Utilities

### `parseAllowedOriginsList`

Source: `src/shared/utils/security/allowed-origins.util.ts`

Parses the comma-separated ALLOWED_ORIGINS env value the same way CORS does.

### `isBearerTokenValid`

Source: `src/shared/utils/security/bearer-token.util.ts`

Constant-time comparison of a Bearer authorization header against an expected token.

### `EncryptedPayload`

Source: `src/shared/utils/security/encryption.util.ts`

<!-- MISSING_DESCRIPTION -->

### `encryptPayload`

Source: `src/shared/utils/security/encryption.util.ts`

Encrypt a plaintext string with AES-256-CBC.

A fresh random 16-byte IV is generated per call so identical plaintexts
produce different ciphertexts.

Client-side decryption (crypto-js, browser):
```js
const key = CryptoJS.enc.Hex.parse(keyHex);
const iv  = CryptoJS.enc.Base64.parse(response.iv);
const decrypted = CryptoJS.AES.decrypt(response.payload, key, {
  iv, mode: CryptoJS.mode.CBC, padding: CryptoJS.pad.Pkcs7,
});
JSON.parse(decrypted.toString(CryptoJS.enc.Utf8));
```

### `decryptFieldSecret`

Source: `src/shared/utils/security/field-secret-encryption.util.ts`

<!-- MISSING_DESCRIPTION -->

### `encryptFieldSecret`

Source: `src/shared/utils/security/field-secret-encryption.util.ts`

Encrypts a short secret for at-rest storage (MFA seeds, webhook signing keys).
Legacy plaintext values are still readable via decryptFieldSecret.

### `resetJwtCachesForTests`

Source: `src/shared/utils/security/jwt.util.ts`

Test-only reset — avoids bleed between Vitest cases that change JWT env.

### `signAccessToken`

Source: `src/shared/utils/security/jwt.util.ts`

Sign an access token with RS256.
- 15-minute expiry (or shorter for global admin)
- No email in payload (security: avoid leaking PII)
- iss/aud claims set for validation

### `TokenPayload`

Source: `src/shared/utils/security/jwt.util.ts`

<!-- MISSING_DESCRIPTION -->

### `verifyAccessToken`

Source: `src/shared/utils/security/jwt.util.ts`

<!-- MISSING_DESCRIPTION -->

### `hashPassword`

Source: `src/shared/utils/security/password.util.ts`

Hash a password with Argon2id.

### `verifyPassword`

Source: `src/shared/utils/security/password.util.ts`

Verify a password against a stored Argon2id hash.
Returns `needsRehash` when Argon2 parameters should be upgraded.

### `isSensitiveKey`

Source: `src/shared/utils/security/sensitive-redaction.util.ts`

<!-- MISSING_DESCRIPTION -->

### `redactSensitive`

Source: `src/shared/utils/security/sensitive-redaction.util.ts`

<!-- MISSING_DESCRIPTION -->

### `redactSensitiveQueryString`

Source: `src/shared/utils/security/sensitive-redaction.util.ts`

Redacts sensitive query parameter values in an application/x-www-form-urlencoded string.

### `redactSensitiveUrl`

Source: `src/shared/utils/security/sensitive-redaction.util.ts`

Redacts sensitive query parameter values in a full URL string.

### `SENSITIVE_REDACTION_PLACEHOLDER`

Source: `src/shared/utils/security/sensitive-redaction.util.ts`

Central recursive, case-insensitive secret redactor.

Walks arbitrary objects/arrays and replaces the value of any key whose name contains a
sensitive fragment (case-insensitive substring) with `[REDACTED]`. Used by the Pino logger
and Sentry `beforeSend` so headers, query, body, breadcrumbs, and extras are scrubbed
regardless of nesting depth or header casing.

String values that look like URLs or query strings have sensitive parameter values scrubbed.
Returns a redacted deep copy — never mutates the input. Cyclic references are tracked via
WeakMap so the original object graph is never returned from a cycle or depth boundary.

### `TurnstileVerifyResult`

Source: `src/shared/utils/security/turnstile-verifier.util.ts`

<!-- MISSING_DESCRIPTION -->

### `verifyTurnstileToken`

Source: `src/shared/utils/security/turnstile-verifier.util.ts`

Verifies a Cloudflare Turnstile token via siteverify API.

### `VerifyTurnstileTokenOptions`

Source: `src/shared/utils/security/turnstile-verifier.util.ts`

<!-- MISSING_DESCRIPTION -->

### `createPinnedWebhookFetch`

Source: `src/shared/utils/security/webhook-outbound-fetch.util.ts`

Returns a fetch implementation that connects to the IP from a single pre-delivery DNS resolution
while preserving the original Host header and TLS SNI.

### `PinnedWebhookFetch`

Source: `src/shared/utils/security/webhook-outbound-fetch.util.ts`

<!-- MISSING_DESCRIPTION -->

### `PinnedWebhookResolution`

Source: `src/shared/utils/security/webhook-outbound-fetch.util.ts`

<!-- MISSING_DESCRIPTION -->

### `resolveAndPinWebhookUrl`

Source: `src/shared/utils/security/webhook-outbound-fetch.util.ts`

Resolves webhook DNS once, validates SSRF rules + optional hostname allowlist, and returns a pinned target.
Outbound delivery must use this (or {@link createPinnedWebhookFetch}) so DNS rebinding cannot redirect fetches.

### `buildWebhookSignatureHeader`

Source: `src/shared/utils/security/webhook-signature.util.ts`

Builds the `X-Webhook-Signature` header value: `t=<timestamp>,v1=<signature>`.

### `signWebhookPayload`

Source: `src/shared/utils/security/webhook-signature.util.ts`

HMAC-SHA256 signature for outbound webhook payloads (`timestamp.payload` convention).

### `verifyWebhookPayloadSignature`

Source: `src/shared/utils/security/webhook-signature.util.ts`

Constant-time verification of a hex signature from {@link signWebhookPayload}.

### `assertWebhookUrlSafe`

Source: `src/shared/utils/security/webhook-url.util.ts`

Validates a webhook URL for SSRF safety and returns resolved addresses (single DNS lookup).
Rejects non-HTTP(S) schemes, blocked hostnames, and private/link-local resolved IPs.

### `resolveWebhookUrlAddresses`

Source: `src/shared/utils/security/webhook-url.util.ts`

Resolves a webhook hostname once and validates addresses are not private/link-local.

### `validateWebhookUrl`

Source: `src/shared/utils/security/webhook-url.util.ts`

Validates a webhook URL for SSRF safety. Rejects:
- Non-HTTP(S) schemes
- Blocked hostnames (localhost, 127.0.0.1, 0.0.0.0)
- Hostnames resolving to private/link-local IPs
- Cloud metadata endpoints (169.254.169.254)

### `WebhookResolvedAddress`

Source: `src/shared/utils/security/webhook-url.util.ts`

<!-- MISSING_DESCRIPTION -->

