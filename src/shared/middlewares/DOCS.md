`src/shared/middlewares/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Middlewares — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Routes

| Method | Path | Access | Description |
| --- | --- | --- | --- |
| GET | `/health` | PUBLIC | <!-- MISSING_DESCRIPTION --> |

## Middlewares

### `applyApiKeyAuthentication`

Source: `src/shared/middlewares/api-key-auth.middleware.ts`

Authenticates the request with an organization API key when present.
Returns true when API key auth succeeded; false when no API key was sent.
Throws when a key was sent but is invalid.

### `extractApiKeyFromRequest`

Source: `src/shared/middlewares/api-key-auth.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `captchaPreHandler`

Source: `src/shared/middlewares/captcha.middleware.ts`

Validates Cloudflare Turnstile token from X-Captcha-Token on public auth routes.
Skipped when CAPTCHA_PROVIDER=disabled or secret unset (fail-open in dev/test only).

### `attachRequestI18nHelpers`

Source: `src/shared/middlewares/i18n.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `resolveRequestLanguageFromHeader`

Source: `src/shared/middlewares/i18n.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `verifyLocalesAvailable`

Source: `src/shared/middlewares/i18n.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `idempotencyOnResponse`

Source: `src/shared/middlewares/idempotency.middleware.ts`

Final-step Redis write. By running here (rather than in `onSend`) the completed cache
is only persisted after the request-level transaction has resolved — preventing the
documented "rolled-back write replays as 2xx" failure mode. Three terminal cases:

 1. statusCode < 400 + pending completion stashed -> overwrite placeholder with completed entry
 2. statusCode >= 400 or pending completion absent (handler threw, body too large) -> DEL
    placeholder so the client may safely retry
 3. claim was never acquired -> no-op

> TSDoc parser warnings:
> - (6,19): Invalid HTML element: A space is not allowed here
> - (6,55): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag
> - (7,19): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag
> - (7,88): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag
> - (9,34): The ">" character should be escaped using a backslash to avoid confusion with an HTML tag

### `IdempotencyOnResponseOptions`

Source: `src/shared/middlewares/idempotency.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `OrganizationRlsTransactionSettlementOutcome`

Source: `src/shared/middlewares/organization-rls-transaction.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `settleAndAwaitOrganizationRlsTransaction`

Source: `src/shared/middlewares/organization-rls-transaction.middleware.ts`

Signals settlement to the in-flight RLS transaction and awaits the outer
`database.transaction()` promise so commit/rollback has finished before the caller
proceeds. Also releases the pooled checkout counter. Safe to call for non-org
requests (no-op when no transaction was opened).

Intended to be invoked exclusively from the request lifecycle coordinator
(`request-lifecycle.middleware.ts`) — it owns the post-response ordering across RLS,
idempotency, and outbox flush.

### `formatResponsePayload`

Source: `src/shared/middlewares/response-format.middleware.ts`

<!-- MISSING_DESCRIPTION -->

### `isPaddleEnvelope`

Source: `src/shared/middlewares/response-format.middleware.ts`

Paddle-style envelope: { data, meta: { request_id, ... } }

> TSDoc parser warnings:
> - (1,28): Expecting a TSDoc tag starting with "{@"
> - (1,42): Expecting a TSDoc tag starting with "{@"
> - (1,60): The "}" character should be escaped using a backslash to avoid confusion with a TSDoc inline tag
> - (1,62): The "}" character should be escaped using a backslash to avoid confusion with a TSDoc inline tag

### `parseOrganizationPublicIdFromUrl`

Source: `src/shared/middlewares/tenant.middleware.ts`

<!-- MISSING_DESCRIPTION -->

## Constants

### `EXPENSIVE_AUTHED_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Expensive operations (5 req / 5 min), keyed by user when possible.

### `MODERATE_AUTHED_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Moderate cap for authenticated operations that send email or mint URLs (30 req / 60s).

### `ORGANIZATION_SCOPED_AUTHED_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Organization-scoped mutations (100 req / 60s), keyed by X-Organization-Id when set.

### `REFRESH_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Token refresh (30 req / 60s), keyed by IP — runs on the default hook (onRequest).

### `STRICT_AUTHED_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Authenticated credential / abuse-sensitive mutations (10 req / 60s in production), keyed by
user when possible. The cap is lifted under NODE_ENV=test so suites that loop through password
change / MFA flows from a single loopback IP do not produce flaky 429s.

### `STRICT_PUBLIC_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Sensitive public auth-style endpoints — strict cap keyed by IP.

### `WEBHOOK_RATE_LIMIT`

Source: `src/shared/middlewares/rate-limit-presets.constants.ts`

Stripe webhook ingress (60 req / 60s per IP) — signature verified but cap abuse.

## Index

### `middlewarePlugins`

Source: `src/shared/middlewares/index.ts`

<!-- MISSING_DESCRIPTION -->

### `registerMiddleware`

Source: `src/shared/middlewares/index.ts`

<!-- MISSING_DESCRIPTION -->

## Other

### `requireAllowedOriginWhenPresentForCookieSessionRoute`

Source: `src/shared/middlewares/cookie-session-origin.pre-handler.ts`

@deprecated Use {@link requireAllowedSourceOriginForCookieSessionRoute}.

### `requireAllowedSourceOriginForCookieSessionRoute`

Source: `src/shared/middlewares/cookie-session-origin.pre-handler.ts`

For routes that authenticate via httpOnly session cookies: when ALLOWED_ORIGINS is configured,
require a trusted source origin from the Origin header. In production, when Origin is absent,
require CSRF double-submit (X-CSRF-Token header matching csrf_token cookie). Non-production
may fall back to Referer origin validation. Empty allowlist skips the check (e.g. some dev setups).

