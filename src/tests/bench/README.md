# Microbenchmarks (`*.bench.ts`)

Autocannon scripts for single-endpoint throughput checks. Not run by Vitest.

```bash
pnpm test:bench
```

For deployed load testing use k6 under `src/tests/load/k6/`.
