`src/domains/audit/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Audit — Catalog

## Overview

Append-only audit log of every security- and governance-relevant action that happens on the platform: who did it (actor), what they did (action), to what (resource), from where (IP, user-agent), with what severity, and any structured metadata. The domain owns the canonical write path used by the `audit-emission` cross-cutting pattern, and exposes a global-admin-only read API for forensic and compliance queries.

→ Read the full overview: [OVERVIEW.md](./OVERVIEW.md)

## Routes

| Method | Path | Access | Description |
| --- | --- | --- | --- |
| GET | `/api/v1/audit/logs` | ROLE: super_admin, admin | Returns audit log entries with cursor pagination (`after`, `limit`). Requires SUPER_ADMIN or ADMIN role. |

## Controllers

### `createAuditController`

Source: `src/domains/audit/audit.controller.ts`

<!-- MISSING_DESCRIPTION -->

## Services

### `AuditService`

Source: `src/domains/audit/audit.service.ts`

Owns the canonical write + read paths for the audit log.

<details><summary>Business logic</summary>

Algorithm:
1. {@link AuditService.record} resolves the actor's internal user id from
   their public id inside `withUserDatabaseContext` so RLS sees the actor's
   organization scope, then inserts the audit row in the same context.
2. {@link AuditService.list} validates query input via Zod, resolves
   organization / actor public ids to internal ids, and delegates to
   {@link AuditRepository.findWithFilters} for cursor-paginated reads.

Failure modes:
- Unknown actor public id → logged at `warn`; no row written; caller is
  unaffected (writes are best-effort by contract).
- DB write failure inside the context → bubbles to the caller; callers
  should go through `recordAuditEvent` so the failure is caught and logged.

Side effects: one INSERT into `audit_logs.audit_log`. No events emitted —
audit is a write target, not an emitter.

Notes: callers must never assume immediate-read-your-write through a
different organization context, because the row is RLS-scoped to the actor.

</details>

## Repositories

### `AuditRepository`

Source: `src/domains/audit/audit.repository.ts`

<!-- MISSING_DESCRIPTION -->

## Schemas

### `AuditLogInsert`

Source: `src/domains/audit/audit.schema.ts`

<!-- MISSING_DESCRIPTION -->

### `logs`

Source: `src/domains/audit/audit.schema.ts`

<!-- MISSING_DESCRIPTION -->

## DTOs

### `ListAuditLogsQuery`

Source: `src/domains/audit/audit.dto.ts`

<!-- MISSING_DESCRIPTION -->

### `ListAuditLogsQueryDto`

Source: `src/domains/audit/audit.dto.ts`

<!-- MISSING_DESCRIPTION -->

## Validators

### `validateListAuditLogsQuery`

Source: `src/domains/audit/audit.validator.ts`

<!-- MISSING_DESCRIPTION -->

## Serializers

### `AuditSerializer`

Source: `src/domains/audit/audit.serializer.ts`

<!-- MISSING_DESCRIPTION -->

## Containers

### `AuditContainer`

Source: `src/domains/audit/audit.container.ts`

<!-- MISSING_DESCRIPTION -->

### `createAuditContainer`

Source: `src/domains/audit/audit.container.ts`

<!-- MISSING_DESCRIPTION -->

### `registerAuditContainer`

Source: `src/domains/audit/audit.container.ts`

<!-- MISSING_DESCRIPTION -->

## Types

### `AuditLogFilters`

Source: `src/domains/audit/audit.types.ts`

<!-- MISSING_DESCRIPTION -->

### `AuditLogRecordInput`

Source: `src/domains/audit/audit.types.ts`

Server-internal audit input: resolves `actor_user_id` from the user's public id.

### `NewAuditLog`

Source: `src/domains/audit/audit.types.ts`

Row shape for inserting into `audit.logs` (Drizzle insert).

## Seeds

### `SeedAuditLogPayload`

Source: `src/domains/audit/audit.seed.ts`

<!-- MISSING_DESCRIPTION -->

### `seedAuditLogs`

Source: `src/domains/audit/audit.seed.ts`

<!-- MISSING_DESCRIPTION -->

## Other

### `AUDIT_PERMISSIONS`

Source: `src/domains/audit/audit.permissions.ts`

<!-- MISSING_DESCRIPTION -->

