`src/domains/notify/sub-domains/webhook/workers/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Workers — Catalog

## Overview

<!-- MISSING_DESCRIPTION -->

Overview missing — author `src/domains/notify/sub-domains/webhook/workers/OVERVIEW.md` (variant A.2-sub-domain).

## Workers

### `createWebhookDeliveryWorker`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-delivery.worker.ts`

Creates a BullMQ worker that delivers outbound webhooks with HMAC signing.

<!-- MISSING_REMARKS -->

Public export in `webhook-delivery.worker.ts` requires a `@remarks` block.

### `processWebhookDeliveryAttempt`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-delivery.worker.ts`

Delivers a single webhook attempt (testable with an injected fetch implementation).

<!-- MISSING_REMARKS -->

Public export in `webhook-delivery.worker.ts` requires a `@remarks` block.

### `WebhookDeliveryFetch`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-delivery.worker.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `webhook-delivery.worker.ts` requires a `@remarks` block.

### `createWebhookTombstoneRetentionWorker`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-tombstone-retention.worker.ts`

Hard-delete notification webhooks tombstoned longer than TOMBSTONE_RETENTION_DAYS.
Cascade removes webhook_delivery_attempts (FK ON DELETE CASCADE).
Repeatable schedule is registered in `src/infrastructure/queue/scheduler.ts`.

<!-- MISSING_REMARKS -->

Public export in `webhook-tombstone-retention.worker.ts` requires a `@remarks` block.

## Processors

### `runWebhookTombstoneRetentionJob`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-tombstone-retention.processor.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `webhook-tombstone-retention.processor.ts` requires a `@remarks` block.

## Constants

### `WEBHOOK_TOMBSTONE_RETENTION_QUEUE_NAME`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-tombstone-retention.constants.ts`

BullMQ queue name — repeatable schedule: `src/infrastructure/queue/scheduler.ts`.

## Other

### `fetchWebhookWithCircuitBreaker`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-outbound-circuit.ts`

<!-- MISSING_DESCRIPTION -->

### `resetWebhookOutboundCircuitsForTesting`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-outbound-circuit.ts`

Clears per-URL circuits between tests (not for production use).

### `webhookDeliveryBackoffWithJitter`

Source: `src/domains/notify/sub-domains/webhook/workers/webhook-outbound-circuit.ts`

Exponential backoff with up to 30% jitter (BullMQ custom backoff for webhook-delivery).

