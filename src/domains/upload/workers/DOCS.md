`src/domains/upload/workers/`

> Auto-generated by `pnpm features:generate`. Do not edit by hand.

# Workers — Catalog

## Overview

(No `OVERVIEW.md` for this folder.)

## Workers

### `createUploadPendingSweepWorker`

Source: `src/domains/upload/workers/upload-pending-sweep.worker.ts`

Reconciles orphan PENDING uploads (presigned URL issued, confirm never called).
Repeatable schedule: src/infrastructure/queue/scheduler.ts.

<!-- MISSING_REMARKS -->

Public export in `upload-pending-sweep.worker.ts` requires a `@remarks` block.

### `createUploadTombstoneRetentionWorker`

Source: `src/domains/upload/workers/upload-tombstone-retention.worker.ts`

Hard-delete uploads tombstoned longer than TOMBSTONE_RETENTION_DAYS.
Removes S3 objects before deleting rows.

<!-- MISSING_REMARKS -->

Public export in `upload-tombstone-retention.worker.ts` requires a `@remarks` block.

## Processors

### `runUploadPendingSweepJob`

Source: `src/domains/upload/workers/upload-pending-sweep.processor.ts`

Reconciles stale PENDING uploads — clients that obtained a presigned URL but never
called confirm. For each candidate row past the grace window:

- Object exists with matching size  → mark UPLOADED (auto-confirm).
- Object exists with mismatched size → mark FAILED (will not be attachable).
- Object missing                    → hard-delete the orphan row (idempotent S3 delete first).

Runs under withGlobalRetentionCleanupDatabaseContext so RLS does not block cross-tenant rows.

<!-- MISSING_REMARKS -->

Public export in `upload-pending-sweep.processor.ts` requires a `@remarks` block.

### `UploadPendingSweepResult`

Source: `src/domains/upload/workers/upload-pending-sweep.processor.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `upload-pending-sweep.processor.ts` requires a `@remarks` block.

### `runUploadTombstoneRetentionJob`

Source: `src/domains/upload/workers/upload-tombstone-retention.processor.ts`

<!-- MISSING_DESCRIPTION -->

<!-- MISSING_REMARKS -->

Public export in `upload-tombstone-retention.processor.ts` requires a `@remarks` block.

## Constants

### `UPLOAD_PENDING_SWEEP_BATCH_SIZE`

Source: `src/domains/upload/workers/upload-pending-sweep.constants.ts`

Maximum rows scanned per sweeper invocation.

### `UPLOAD_PENDING_SWEEP_QUEUE_NAME`

Source: `src/domains/upload/workers/upload-pending-sweep.constants.ts`

BullMQ queue for auto-confirming or hard-deleting upload.uploads rows stuck in PENDING.

### `UPLOAD_TOMBSTONE_RETENTION_QUEUE_NAME`

Source: `src/domains/upload/workers/upload-tombstone-retention.constants.ts`

BullMQ queue for hard-deleting upload.uploads tombstones past TOMBSTONE_RETENTION_DAYS.

