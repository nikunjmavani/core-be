# API documentation hub (core-be)

How OpenAPI specs, interactive reference UI, Postman collections, and the Scalar Registry fit together.

---

## Artifacts

| Artifact | Location | Generated by | Hand-edited |
| -------- | -------- | ------------ | ----------- |
| OpenAPI 3.0 (en/es) | `docs/openapi/openapi.json`, `openapi.es.json` | `pnpm docs:generate` / `docs:generate:multilang` | No |
| Postman Collection v2.1 | `docs/postman-collection.json` | `pnpm docs:postman` | No |
| Route catalog | `docs/routes.txt` | `pnpm routes:catalog` | No |
| Interactive reference | `GET /reference` (when enabled) | Runtime from `OPENAPI_SPEC_PATH` | No |

Regenerate after route or schema changes with **`pnpm docs:all`** (or `pnpm docs:generate:multilang` then `pnpm docs:postman`). CI runs **`pnpm docs:check`**, which regenerates gitignored `docs/openapi/` and `docs/postman-collection.json` and fails if output would change—there is no committed spec to `git diff`.

The generator scans all domain `*.routes.ts` files for `zodApplication.get/post/...` (and legacy `app.*`) registrations, maps request bodies via `tooling/openapi/schema-map.ts`, query parameters via `tooling/openapi/query-schema-map.ts`, and omits JSON bodies for routes in `tooling/openapi/routes-without-json-body.ts` (no bare `{type:'object'}` fallback). Global regression: `pnpm vitest run --project global src/tests/global/openapi-completeness.global.test.ts` and `src/tests/global/openapi-cursor-pagination.global.test.ts`.

### Cursor pagination in OpenAPI

List endpoints that use cursor pagination document these **query parameters**:

| Parameter | Description |
| --------- | ----------- |
| `limit` | Page size (default 25, max 100) |
| `after` | Opaque cursor from the previous response `meta.pagination.next` |
| `page` | **Deprecated** offset page (see [api-versioning.md](../api/api-versioning.md)) |

Responses use `meta.pagination`: `per_page`, `next` (cursor or null), `has_more`, and optional `estimated_total`. The canonical route list is `tooling/openapi/pagination-openapi.ts` (`CURSOR_PAGINATED_LIST_ROUTE_KEYS`); query DTOs are mapped in `tooling/openapi/query-schema-map.ts`.

---

## Local interactive reference (Scalar)

Set in `.env`:

```bash
ENABLE_API_REFERENCE=true
# OPENAPI_SPEC_PATH=docs/openapi/openapi.json   # optional; this is the default
```

Prerequisites:

1. `pnpm docs:generate` — produces `docs/openapi/openapi.json`
2. `pnpm dev` — API serves Scalar at [http://localhost:3000/reference/](http://localhost:3000/reference/)

The generated spec includes **MCP** routes (`GET`/`POST /api/v1/mcp`), root `x-mcp` metadata (tools `call_api`, resources `core-be://openapi` and `core-be://routes`), and `components.schemas.CallApiToolInput`. When `ENABLE_MCP_SERVER=true`, Scalar also links the live MCP endpoint at `/api/v1/mcp`.

The UI also exposes the spec at `/reference/openapi.json` and `/reference/openapi.yaml`.

---

## Validation

```bash
pnpm docs:generate
pnpm docs:validate:openapi
```

Uses `@scalar/cli` (`scalar document validate`) against `OPENAPI_SPEC_PATH` (default `docs/openapi/openapi.json`).

---

## Hosted uploads (CI after merge)

On **push** to **`dev`** or **`main`** (after Quality passes), CI regenerates specs and uploads to the matching **GitHub Environment** secrets:

| Branch | GitHub Environment | Typical `SCALAR_SLUG` |
| ------ | ------------------ | --------------------- |
| `dev` | `development` | `core-be-dev` |
| `main` | `production` | `core-be` |

| Target | Command | Secrets / env (per GitHub Environment) |
| ------ | ------- | -------------------------------------- |
| Postman workspace | `pnpm docs:upload` | `POSTMAN_API_KEY`, `POSTMAN_WORKSPACE_ID` |
| Scalar Registry | `pnpm docs:upload:scalar` | `SCALAR_API_KEY`, `SCALAR_NAMESPACE`; optional `SCALAR_SLUG` (default `core-be`) |
| Both | `pnpm docs:upload:hosted` | All of the above |

Registry URL pattern:

`https://registry.scalar.com/@<namespace>/apis/<slug>/latest`

When `SCALAR_NAMESPACE` is set during `pnpm docs:postman`, the Postman collection description includes this link.

---

## Full docs pipeline

```bash
pnpm docs:all                  # OpenAPI (en + es) + Postman
pnpm docs:validate:openapi     # Scalar CLI validation
pnpm docs:upload:hosted        # Postman + Scalar Registry (needs secrets)
```

**Before merge (PR):** `pnpm routes:catalog:check` and `pnpm docs:check` run in **CI / Quality & static security** (required on PRs to `dev`, `main`). Pre-commit also regenerates `docs/routes.txt` when routes change.

**After merge (push):** CI ([`.github/workflows/reusable/docs-generate.yml`](../../../.github/workflows/reusable/docs-generate.yml)) runs `docs:all`, `docs:validate:openapi`, then uploads to Postman and Scalar Registry when environment secrets are set.

---

## Related

- [domains-and-public-api-design.md](../architecture/domains-and-public-api-design.md) — route layout and response shape
- [api-versioning.md](../api/api-versioning.md) — `/api/v1` and deprecation headers
- [integrations/cursor-backend-mcp.md](../../integrations/cursor-backend-mcp.md) — MCP tools backed by the same OpenAPI spec
- [documentation-system.md](../architecture/documentation-system.md) — doc ownership map
