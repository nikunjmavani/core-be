# Dependencies
node_modules/
**/node_modules/

# pnpm
.pnpm-store/

# Build
dist/

# Environment: root only — .env (local, never commit); .env.example (committed)
.env
.env.local
.env.development
.env.production
.env.*
!.env.example
!.env.local.example
!.env.development.example
!.env.production.example

# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/vitest.workspace.ts
.idea/
*.swp
*.swo
*~
*.bak

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Coverage
coverage/

# Generated docs (run pnpm docs:generate / pnpm docs:postman to recreate)
docs/openapi/openapi.json
docs/openapi/openapi.*.json
docs/postman-collection.json

# Vite/Vitest caches (should never be in repo)
**/.vite/
src/tests/node_modules/

# Stryker mutation-testing sandboxes (local only)
.stryker-tmp/

# Security scan reports
.semgrep/

# Local-only reports (do not commit)
# Generic report files generated by tools/skills/rules
TEST_REPORT.md

# User-specific Cursor config (may contain API keys)
.cursor/mcp.json

# Claude Code worktrees (local agent sandboxes; never commit)
.claude/
reports/
**/reports/
**/*-report.*
**/*.report.*
*REPORT*.md

# Setup automation (secrets + provisioned state)
tooling/setup.secrets.json
.env.setup
.setup-state.json
